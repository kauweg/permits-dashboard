const state={meta:null,summary:null,selectedSlice:null,map:null,mapLayer:null};
const byId=id=>document.getElementById(id);
const fmt=n=>new Intl.NumberFormat().format(Number(n||0));
const esc=s=>String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]));
const qs=o=>new URLSearchParams(o).toString();
async function fetchJson(url){const r=await fetch(url,{cache:"no-store"});if(!r.ok)throw new Error(`${r.status} ${r.statusText}`);return r.json();}
function filters(){return{jurisdiction:byId("jurisdiction").value,category:byId("category").value,market:byId("market").value,neighborhood:byId("neighborhood").value,start_year:byId("startYear").value,end_year:byId("endYear").value};}
function loadMsgs(notes,errors){byId("loadNotes").innerHTML=(notes||[]).map(n=>`<span class="note-pill">${esc(n)}</span>`).join("");byId("loadErrors").innerHTML=(errors||[]).map(e=>`<div>${esc(e)}</div>`).join("");}
function populate(id,items,label){const s=byId(id),cur=s.value||"all";s.innerHTML=`<option value="all">${label}</option>`;(items||[]).forEach(x=>{const o=document.createElement("option");o.value=x;o.textContent=x;s.appendChild(o);});if([...s.options].some(o=>o.value===cur))s.value=cur;}
async function loadMeta(){state.meta=await fetchJson("/api/meta");populate("market",state.meta.markets||[],"All markets");populate("neighborhood",state.meta.neighborhoods||[],"All neighborhoods");loadMsgs(state.meta.load_notes||[],state.meta.load_errors||[]);}
async function loadSummary(){state.summary=await fetchJson(`/api/summary?${qs(filters())}`);loadMsgs(state.summary.load_notes||[],state.summary.load_errors||[]);renderCards();renderChart();renderMarkets();renderNeighborhoods();renderMap();}
function mapPoints(){let rows=state.summary?.map_points||[];if(state.selectedSlice?.year)rows=rows.filter(r=>Number(r.year)===Number(state.selectedSlice.year));if(state.selectedSlice?.category)rows=rows.filter(r=>r.category===state.selectedSlice.category);return rows;}
function neighborhoodRows(){let rows=state.summary?.neighborhood_rows||[];if(state.selectedSlice?.year){const y=String(state.selectedSlice.year);rows=rows.filter(r=>Number(r.years?.[y]?.Total||0)>0);}if(state.selectedSlice?.category){const y=state.selectedSlice?.year?String(state.selectedSlice.year):null;rows=rows.filter(r=>y?Number(r.years?.[y]?.[state.selectedSlice.category]||0)>0:Number(r.totals?.[state.selectedSlice.category]||0)>0);}return rows;}
function renderCards(){const pts=mapPoints(),c=state.summary?.cards||{};const items=[["Visible Permits",pts.length||c.total_permits||0],["Known Units",pts.reduce((a,p)=>a+Number(p.units||0),0)||c.known_units||0],["Est. Units",pts.reduce((a,p)=>a+Number(p.estimated_units||0),0)||c.estimated_units||0],["Neighborhoods",new Set(pts.map(p=>p.raw_neighborhood).filter(Boolean)).size||c.known_neighborhoods||0],["Markets",new Set(pts.map(p=>p.market).filter(Boolean)).size||c.known_markets||0],["New MF",pts.filter(p=>p.category==="New MF").length||c.new_mf||0],["New SFR",pts.filter(p=>p.category==="New SFR").length||c.new_sfr||0],["Demo",pts.filter(p=>p.category==="Demo").length||c.demo||0]];byId("cards").innerHTML=items.map(([l,v])=>`<article class="card executive-card"><div class="card-label">${esc(l)}</div><div class="card-value">${fmt(v)}</div></article>`).join("");}
function drawBars(canvas,labels,series,click){const ctx=canvas.getContext("2d"),rect=canvas.getBoundingClientRect(),w=Math.max(300,Math.floor(rect.width)),h=260,ratio=window.devicePixelRatio||1;canvas.width=w*ratio;canvas.height=h*ratio;ctx.setTransform(ratio,0,0,ratio,0,0);ctx.clearRect(0,0,w,h);const m={l:48,r:20,t:18,b:42},pw=w-m.l-m.r,ph=h-m.t-m.b,colors=["#5b7fa6","#7fa7d6","#c48b4d"],max=Math.max(1,...series.flatMap(s=>s.values));ctx.strokeStyle="#d6dee8";ctx.fillStyle="#667085";ctx.font="12px Arial";for(let i=0;i<=4;i++){const y=m.t+ph-(ph*i/4);ctx.beginPath();ctx.moveTo(m.l,y);ctx.lineTo(w-m.r,y);ctx.stroke();ctx.fillText(String(Math.round(max*i/4)),10,y+4);}const gw=pw/Math.max(1,labels.length),bw=Math.min(24,Math.max(10,(gw-12)/series.length)),hits=[];labels.forEach((label,li)=>{const gx=m.l+li*gw+6;series.forEach((s,si)=>{const val=Number(s.values[li]||0),bh=(val/max)*ph,x=gx+si*bw,y=m.t+ph-bh;ctx.fillStyle=colors[si%colors.length];ctx.fillRect(x,y,bw-2,bh);hits.push({x,y,w:bw-2,h:bh,label,series:s.name,value:val});});ctx.fillStyle="#334155";ctx.fillText(String(label),gx,h-10);});canvas.onclick=e=>{const r=canvas.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top,hit=hits.find(h=>x>=h.x&&x<=h.x+h.w&&y>=h.y&&y<=h.y+h.h);if(hit)click(hit);};}
function renderChart(){const rows=state.summary?.annual_series||[];drawBars(byId("annualChart"),rows.map(r=>r.year),[{name:"New SFR",values:rows.map(r=>r["New SFR"]||0)},{name:"New MF",values:rows.map(r=>r["New MF"]||0)},{name:"Demo",values:rows.map(r=>r.Demo||0)}],hit=>{state.selectedSlice={year:Number(hit.label),category:hit.series};byId("annualChartValue").textContent=`${hit.label} • ${hit.series}: ${fmt(hit.value)}`;renderCards();renderNeighborhoods();renderMap();});}
function badgeClass(v){v=String(v||"").toLowerCase();if(v.includes("saturated")||v.includes("caution"))return"badge caution";if(v.includes("underserved")||v.includes("opportunity"))return"badge opportunity";if(v.includes("accelerating")||v.includes("heating"))return"badge active";return"badge";}
function renderMarkets(){const tbody=byId("marketTable").querySelector("tbody"),rows=state.summary?.market_rows||[];tbody.innerHTML=rows.map(r=>`<tr data-market="${esc(r.name)}"><td>${esc(r.name)}</td><td>${fmt(r.totals?.Total)}</td><td>${fmt(r.totals?.["Known Units"])}</td><td>${fmt(r.totals?.["Estimated Units"])}</td><td><span class="${badgeClass(r.trajectory)}">${esc(r.trajectory)}</span></td><td><span class="${badgeClass(r.opportunity)}">${esc(r.opportunity)}</span></td></tr>`).join("");tbody.querySelectorAll("tr").forEach(tr=>tr.onclick=async()=>{byId("market").value=tr.dataset.market;byId("neighborhood").value="all";state.selectedSlice=null;await loadSummary();byId("mapContext").textContent=`${tr.dataset.market} • all neighborhoods`;});}
function renderNeighborhoods(){const tbody=byId("neighborhoodTable").querySelector("tbody"),rows=neighborhoodRows();tbody.innerHTML=rows.map(r=>`<tr data-neighborhood="${esc(r.name)}"><td>${esc(r.name)}</td><td>${esc(r.market)}</td><td>${fmt(r.years?.["2022"]?.Total)}</td><td>${fmt(r.years?.["2023"]?.Total)}</td><td>${fmt(r.years?.["2024"]?.Total)}</td><td>${fmt(r.years?.["2025"]?.Total)}</td><td>${fmt(r.years?.["2026"]?.Total)}</td><td>${fmt(r.totals?.Total)}</td><td>${fmt(r.totals?.["Known Units"])}</td><td>${fmt(r.totals?.["Estimated Units"])}</td><td><span class="${badgeClass(r.trajectory)}">${esc(r.trajectory)}</span></td><td><span class="${badgeClass(r.opportunity)}">${esc(r.opportunity)}</span></td></tr>`).join("");if(!rows.length)tbody.innerHTML=`<tr><td colspan="12" class="muted">No neighborhoods in this selection.</td></tr>`;tbody.querySelectorAll("tr[data-neighborhood]").forEach(tr=>tr.onclick=async()=>{byId("neighborhood").value=tr.dataset.neighborhood;state.selectedSlice=null;await loadSummary();byId("mapContext").textContent=`${tr.dataset.neighborhood} • selected neighborhood`;});}
function initMap(){if(state.map)return;state.map=L.map("permitMap",{preferCanvas:true}).setView([47.6062,-122.3321],11);L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",{maxZoom:19,attribution:"&copy; OpenStreetMap"}).addTo(state.map);state.mapLayer=L.layerGroup().addTo(state.map);}
function fallbackCenter(m){const c={"Downtown Seattle":[47.608,-122.335],"First Hill / Capitol Hill":[47.62,-122.32],"South Lake Union / Eastlake":[47.631,-122.334],"Queen Anne / Magnolia":[47.639,-122.371],"Ballard":[47.668,-122.386],"Fremont / Wallingford":[47.654,-122.346],"University District / Northeast":[47.662,-122.307],"North Seattle":[47.705,-122.343],"Central Seattle":[47.608,-122.303],"Beacon Hill":[47.579,-122.312],"West Seattle":[47.571,-122.386],"South Seattle":[47.555,-122.285],"Bellevue Downtown":[47.6101,-122.2015],"Bellevue":[47.6101,-122.2015],"Unknown":[47.6062,-122.3321]};return c[m]||[47.6062,-122.3321];}
function markerColor(c){return c==="Demo"?"#a16207":c==="New MF"?"#2563eb":"#0f766e";}
function radius(u,e){u=Number(u||0)||Number(e||0);return u>=100?10:u>=50?8:u>=20?6:u>=5?5:4;}
function renderMap(){initMap();state.mapLayer.clearLayers();const rows=mapPoints();if(!rows.length){byId("mapContext").textContent="No points in current selection";return;}const bounds=[];rows.forEach((r,idx)=>{let lat=Number(r.latitude),lng=Number(r.longitude);if(!Number.isFinite(lat)||!Number.isFinite(lng)){const c=fallbackCenter(r.market||"Unknown");lat=c[0]+((idx%11)-5)*.0012;lng=c[1]+((Math.floor(idx/11)%11)-5)*.0012;}const m=L.circleMarker([lat,lng],{radius:radius(r.units,r.estimated_units),color:markerColor(r.category),weight:1,fillColor:markerColor(r.category),fillOpacity:.72});m.bindPopup(`<div class="popup-card"><div class="popup-title">${esc(r.address||"Address unavailable")}</div><div><strong>Market:</strong> ${esc(r.market||"Unknown")}</div><div><strong>Neighborhood:</strong> ${esc(r.raw_neighborhood||"Unknown")}</div><div><strong>Type:</strong> ${esc(r.category||"")}</div><div><strong>Known units:</strong> ${fmt(r.units)}</div><div><strong>Estimated units:</strong> ${fmt(r.estimated_units)}</div><div><strong>Issued:</strong> ${esc((r.issue_date||r.intake_date||"").slice(0,10))}</div></div>`);m.addTo(state.mapLayer);bounds.push([lat,lng]);});bounds.length===1?state.map.setView(bounds[0],14):state.map.fitBounds(bounds,{padding:[18,18]});byId("mapContext").textContent=`${fmt(rows.length)} points shown on map`;}
async function onFilter(){state.selectedSlice=null;await loadSummary();}
function wire(){["jurisdiction","category","market","neighborhood","startYear","endYear"].forEach(id=>byId(id).addEventListener("change",onFilter));}
window.addEventListener("DOMContentLoaded",async()=>{wire();try{await loadMeta();await loadSummary();}catch(e){loadMsgs([],[e.message||String(e)]);console.error(e);}});
